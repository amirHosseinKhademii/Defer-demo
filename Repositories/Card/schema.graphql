type Card {
  id: ID
  header: Header
  body: Body
}

type Header {
  title: String
  followers: Int
  image: String
}

type Body {
  description: String
  todos: [Todo]
}

type Todo {
  id: ID
  title: String
  description: String
  completed: Boolean
}
